# Public default settings (committed to the repository).
#
# Ships disabled — open-source users configure their own relay host/port/password
# in-game via the voice settings menu (default keybind: K).
#
# To make your build auto-connect to a specific relay, create a sibling file
# named `relay-local.properties` in this same directory with `enabled=true` and
# your relay coordinates filled in. That file is gitignored so it won't leak.

enabled=false
host=
port=24454
password=
voiceRange=48
