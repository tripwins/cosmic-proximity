## Build output
.gradle/
build/
out/
classes/
run/

## IDE
.idea/
*.iml
.vscode/

## Private relay coordinates — never commit
src/main/resources/cosmicproximity/relay-local.properties

## Private relay server (standalone). The mod can connect to anything that
## speaks the protocol in `com/cosmicproximity/net/VoicePackets.java`.
## Comment the line below if you want to ship the reference relay too.
src/main/java/com/cosmicproximity/relay/

## Secrets that should never be in version control
*.pem
*.env

## Operational scripts that may contain or load secrets at runtime
scripts/

## Misc
*.log
